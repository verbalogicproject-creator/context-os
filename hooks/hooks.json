{
  "description": "context-os: keeps each architecture map's staleness flag honest — flips a map to DRIFTED when a source file in its folder changes (PostToolUse), and re-checks a map just before it is read so out-of-band changes are caught too (PreToolUse). Never blocks a tool call; the warning lives inside the map file.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/post_tool_use.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_tool_use.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
