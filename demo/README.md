# context-os demo

A tiny two-service app (`api/` in Python, `web/` in TypeScript) used as a worked
example and by the test suite. The `index.ngf.md` + `map-*.ngf.md` files here are
real context-os output: run `/context-os` on any repo to produce the same for yours.
