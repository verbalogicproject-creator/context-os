{
  "description": "context-os: keeps each architecture map's staleness flag honest (flips a map to DRIFTED when a source file in its folder changes), logs map-vs-source use per session so delivered savings can be measured (not guessed), and gently points at a folder's map when the session reaches for source instead. Never blocks a tool call; warnings live inside the map file and in a one-line note.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write|MultiEdit",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/post_tool_use.py\"",
            "timeout": 10
          }
        ]
      }
    ],
    "PreToolUse": [
      {
        "matcher": "Read|Grep|Glob",
        "hooks": [
          {
            "type": "command",
            "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/pre_tool_use.py\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
